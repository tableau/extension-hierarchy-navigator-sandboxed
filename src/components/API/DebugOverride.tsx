// this can now be set from the UI
// however, it is set to false by default so if debug is needed when the extension is first loading
// then set this to true and it will override the UI options setting
export const debugOverride=false;